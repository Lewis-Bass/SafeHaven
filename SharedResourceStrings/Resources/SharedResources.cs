namespace SharedResourceStrings.Resources
{

    /// <summary>
    /// Dummy class that is needed for resource allocations. If another way is ever found scrap it!
    /// The class is needed so that we don't have to create a set of resource files for every controller!
    /// </summary>
    public class SharedResources
    {
    }
}
